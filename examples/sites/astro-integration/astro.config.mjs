import { defineConfig } from 'astro/config';
// In production with `npm install alap`, uncomment the integration:
// import alap from './integrations/alap.mjs';

export default defineConfig({
  // integrations: [
  //   alap({ config: './src/alap-config.ts' }),
  // ],
});
